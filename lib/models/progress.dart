class Progress {
  final String uid;
  final String gameId;
  final String userId;
  final int repetitions;
  final int score;

  Progress(this.uid, this.gameId, this.userId, this.repetitions, this.score);
}
